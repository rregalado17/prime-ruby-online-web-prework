def prime?(numbers)
    numbers.each do |numbers|
    i = 2
      while i < num
        if num % i == 0
          puts "#{numbers} is not a prime"
        else
          puts "#{numbers} is a prime"
        end
        i += 1
      end
   end
end